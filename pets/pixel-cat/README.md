# 像素猫

这是一套可直接复制修改的宠物素材包，包含六种状态，每个状态四帧，以及三段短提示音。PNG 为 160×128 透明背景像素画。

素材由本项目脚本原创生成，可自由使用、修改和分发，无需署名。复现素材：

```sh
uv run scripts/create_pixel_cat.py
```

该命令会重新生成本目录的 PNG、WAV 和 pet.json；定制宠物请先复制到其他目录，避免被覆盖。

规范与安装方式见 [宠物素材指南](../../docs/pet-format.md)。
